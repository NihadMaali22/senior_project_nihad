# ============================================================
# Decision-Making Academic Assistant — Python Dependencies
# ============================================================

# --- Web Framework ---
fastapi[standard]>=0.115.0,<0.116.0
uvicorn[standard]>=0.30.0,<0.32.0
python-multipart>=0.0.9

# --- Database (PostgreSQL + Async SQLAlchemy) ---
sqlalchemy[asyncio]>=2.0.30,<2.1.0
asyncpg>=0.29.0
alembic>=1.13.0

# --- Haystack 2.x Core ---
haystack-ai>=2.5.0,<3.0.0

# --- Haystack Integrations ---
qdrant-haystack>=10.3.0
qdrant-client>=1.13.0,<2.0.0
ollama-haystack>=1.0.0
fastembed-haystack>=1.0.0

# --- Embedding & Reranking ---
sentence-transformers>=3.0.0
fastembed>=0.3.0
transformers>=4.40.0
torch>=2.0.0
accelerate>=0.26.0

# --- Authentication ---
pyjwt>=2.8.0
bcrypt>=4.0.1
bcrypt>=4.1.0

# --- Configuration & HTTP ---
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.2.0
httpx>=0.27.0
elevenlabs>=1.0.0

# --- Document Processing ---
pypdf>=4.2.0
nltk>=3.9.1

# --- Web Crawler (Knowledge Pipeline — run separately, not at app startup) ---
# Install: pip install crawl4ai && crawl4ai-setup
crawl4ai>=0.4.0

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
